node_modules
target

.venv

**/__pycache__

# For agents to download repos and code just to read it
repos_to_explore

# For agents or I to mess around with code temporarily
scratchpad

# Commiting claude memory can be useful, but it risks occupying too much repo space,
# getting stale (especially for memory items that are more broad),
# or leaking credentials
# So, just ignore it
.claude