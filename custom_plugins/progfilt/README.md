# ProgFilt

Stereo identity filter skeleton. Passes audio through unchanged with stereo in → stereo out.

This project includes a WebView-based GUI in the `web-gui` subdirectory.

## Building

From the repository root:

```bash
cargo build --release --features vst3
```
